# 归航计划检索说明

归航计划的内部编号为 `GH-2025-017`，负责人是林晓岚。

## 检索决策

- 混合检索使用倒数排名融合（RRF），基线参数 `k=60`。
- 无模型时仍应提供文件名与全文搜索。
- 任何回答都必须指向真实来源；证据不足时应明确拒答。

## 资源约束

项目预算上限为 286,500 元。低内存机器应依次从完整模式降级到均衡模式、核心模式和安全模式，文件名搜索必须保持可用。

