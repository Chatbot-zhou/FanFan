---
title: 翻翻 V1 方案逐条审查清单
version: 4.2
updated_at: 2026-08-22
status: main 已快进到 origin/main 最新提交 a429582；本地/远端开发分支均已包含在最新 main；Rust 格式、公开错误码契约、README/release manifest 口径、编译、主要单测和本机 Ollama 最小 chat/embed/ps 探针已通过，安装包、完整应用 E2E、当前 DEV/HOLDOUT 评测仍待验收
---

# 翻翻 V1 方案逐条审查清单

本清单以[翻翻 V1 产品与开发规划书](翻翻-产品与开发规划书.md)4.1为需求源。只记录当前代码和本轮实际运行证据，不恢复用户旅程文件，不把旧安装包、历史测试或未完成的评测分数当成新版本证据。历史基线只作为背景，不能替代 2026-08-22 最新代码的当前验收。

状态定义：

- `已验证`：真实目标流程和必要故障路径已运行并有证据；
- `开发完成（待体验）`：代码链路存在且必要检查通过，但尚未由用户实际体验；
- `部分实现`：已有可执行基础，仍有计划内环节未完成；
- `未实现`：当前没有可用实现；
- `暂缓`：规划明确不在当前V1。

## 1. 工作区与边界

| 项目 | 当前事实 |
|---|---|
| 分支 | 当前工作分支为 `main`，已快进到 `origin/main` 最新提交 `a429582`；`codex/docs-onboarding-node`、`codex/feat-desktop-foundation`、`codex/feat-fanfan-runtime`、`codex/refactor-model-config` 均已是最新 `main` 的祖先，无需重复制造合并提交。本地仍保留大量未提交 WIP。 |
| 远端 | `origin = git@github.com:Chatbot-zhou/FanFan.git`，符合 SSH-only 约束。 |
| 当前修改 | 当前 WIP 包含 Ollama runtime/面板/命令、generation/embedding 切向 Ollama tags、安装目录存储、索引重建、Query Planner 确定性快路径、文档级并行召回/RRF，以及本次重新纳入版本管理的两份项目文档；均未提交、未推送。 |
| 评测产物本地化 | 已落地：评测根目录与 RAGAS 输出目录默认定向工作区 `DATA\FanFanEvaluation`；但运行时正式存储默认已切到安装目录 `app-data` / `model-store`，debug 默认仍避免污染源码目录。 |
| 离线判卷 | 已采用：RAGAS 判卷统一走离线 Codex 裁判，私有片段不出机；当前新代码的 DEV/HOLDOUT 判卷尚未完成。 |
| 旧数据 | 新身份 `com.fanfan.desktop` 不读取或迁移旧数据；本轮没有删除旧用户数据。 |
| Git历史 | 尚未重写；未创建提交、标签、PR 或发布。 |
| 最终测试 | 按用户要求仍未启动完整发布测试；本轮不生成安装包，不以 `.artifacts/release` 旧产物验收。 |
| 用户改动 | 已保留原工作区改动；未执行 `git reset --hard`、`git checkout --`、`git clean` 或广域删除。 |
| 交付边界 | 个人维护、无自建服务器；ModelScope 只承载 rerank/OCR/ASR 文件模型，generation/embedding 由本机 Ollama tag 托管。 |
| 判卷方式 | 离线 Codex 裁判：不配置云端评分，不调用任何外部裁判 API，私有片段不出机；临时明文仅当前用户会话解密、不落仓库（见规划书 11.2）。 |

## 2. 本轮实际开发检查

| 检查 | 结果 | 证明范围 |
|---|---|---|
| Git 分支审计 | 通过 | `origin/main` 最新为 `a429582`，本地/远端开发分支均已被 `main` 包含；本轮采用快进同步，没有制造重复 merge commit。 |
| `git diff --check` | 通过 | 当前工作树无空白错误。 |
| `cargo fmt --all --check` | 通过 | 已运行 `cargo fmt --all` 收口 Rust 格式漂移，并复验格式检查通过。 |
| `cargo check -p fanfan-desktop --locked` | 通过 | 桌面后端主包可编译。 |
| `cargo check --workspace --all-targets --locked` | 通过 | Rust 工作区当前目标可编译。 |
| `cargo test -p fanfan-core --locked` | 通过 | 385 passed、0 failed、2 ignored。 |
| `cargo test -p fanfan-desktop --locked` | 通过 | 36 passed、0 failed。 |
| TypeScript `tsc -b` | 通过 | 前端与桥接类型可编译。 |
| 前端 `vitest` | 通过 | 13 文件 / 32 项通过。 |
| 前端生产构建 | 通过 | Vite 构建完成 5165 modules；仍有既存大 chunk 告警。 |
| 公开契约检查 | 通过 | 已补齐 `EVALUATION_DUMP_FAILED`、`EVALUATION_OLLAMA_UNAVAILABLE`，直接运行 `scripts/validate_contracts.py` 通过（catalog=643, emitted=514）。 |
| `pnpm contracts:check` 包装命令 | 环境受阻 | 当前 pnpm 在非 TTY 环境尝试移除 modules 时失败；直接 Python 校验已证明存在真实契约缺口。 |
| Ollama 迁移 | 开发完成（待完整 E2E） | Rust client、Tauri 命令、前端面板、registry 同步、`/api/tags`、`/api/ps`、`/api/pull` 流式进度和取消基础存在；本机已有 `qwen3.5:2b` 与 `qwen3-embedding:0.6b`，`/api/embed` 返回 1024 维，`/api/chat` 最小问答返回有效结果，`/api/ps` 可读；README 与 release manifest 已改为 Ollama 当前口径，评测示例、兼容枚举和旧错误码文案仍需清理或标注。 |
| Query Planner / 文档召回 | 开发完成（待当前评测） | 新增会话感知的确定性快路径、文档级 metadata/semantic 并行召回与 RRF；当前只证明编译/单测通过，尚未用最新链路跑 DEV/HOLDOUT。 |
| 存储默认位置改造 | 开发完成（待安装验证） | 正式版默认安装目录 `app-data` / `model-store`，debug 默认不启用安装盘存储；安装包、Program Files 回退和迁移烟测待验证。 |
| Worker / RAGAS / Clippy / 安装包 / Ollama 完整流程 | 未运行 | 本轮未跑完整 Worker、RAGAS、Clippy、Ollama pull/cancel/start/stop、应用内问答 E2E、压力与安装烟测；只完成了本机 Ollama 最小 chat/embed/ps 探针。 |
| 历史 DEV/RAGAS baseline | 仅历史背景 | 2026-08-20/21 的 baseline 发生在当前 Ollama 与 Query Planner 改动前，不能作为最新版本验收。 |
| TTS/朗读移除残留扫描 | 历史通过，当前未复扫 | 旧结论保留为背景；本轮未做全树 TTS 残留复扫。 |

这些检查不是完整回归，也没有证明真实语音质量、OCR准确率、GPU流畅性、大库稳定性、真实 Ollama 可用性或安装体验。

## 3. 品牌与视觉

| 条目 | 状态 | 当前代码证据 | 未完成 |
|---|---|---|---|
| 中文/英文品牌 | 开发完成（待体验） | 主要当前树使用“翻翻/FanFan”，版本`0.2.0` | 全树与构建产物零残留复扫 |
| 宣传语 | 开发完成（待体验） | 主宣传语“想不起来？翻翻就知道。”，次宣传语“想得到，搜得到，翻翻知道。” | 全页面实际显示确认 |
| 原图资产 | 开发完成（待体验） | 图形标志和字标由用户原始品牌图确定性裁剪；已删除错误重绘SVG | 用户确认裁剪边界、透明底和字标比例 |
| Windows图标 | 部分实现 | PNG/ICO开发尺寸已生成并接入Tauri | 16/20/24/32px逐项视觉、安装器/卸载器/任务栏实机检查 |
| 品牌主题 | 部分实现 | 蓝、紫、粉和深蓝紫语义变量已经替换主要颜色 | 硬编码色清理、全页面白天/夜晚截图和减少动态效果 |
| 技术身份 | 开发完成（待体验） | npm、Rust、Worker、协议、环境变量、应用标识和脚本已切换 | 新数据目录实机、进程名、日志、诊断包和安装目录复验 |
| Git历史清理 | 未实现 | 当前历史仍包含旧品牌，这是有意的安全阶段边界 | 最终测试后bundle、历史重写、受保护强推、全新克隆验证 |

## 4. 本地 AI Runtime

| 条目 | 状态 | 当前代码证据 | 未完成 |
|---|---|---|---|
| Rust Runtime Manager | 部分实现 | `RuntimeManager` 包含任务类型、优先队列、资源预算、租约、取消、压力状态、实例状态和空闲回收 | 持久恢复、所有领域任务覆盖、运行实例心跳和长期统计 |
| 资源优先级 | 开发完成（待体验） | 取消/预览、语音/问答、搜索、索引、OCR/集合分级；前台任务阻止后台抢占 | 真实并发与饥饿测试 |
| 重任务互斥 | 开发完成（待体验） | LLM/VLM/图片理解类重任务全局只允许一个 | 复杂OCR归入重任务后的实机验证 |
| 内存/显存门槛 | 部分实现 | 20%或2GB内存保留、15%或512MB显存保留契约与入场校验存在；ONNX Worker额外按当前空闲显存和默认1536MiB门槛选择CUDA/CPU | 压力恢复、多个GPU选择和边界故障注入 |
| CPU线程预算 | 开发完成（待体验） | 前台最多4线程、后台1至2线程；OCR/语音显式传递线程数 | 5800H及8GB设备真实CPU占用与整机流畅性 |
| Ollama 后端 | 部分实现 | 本机 `127.0.0.1:11434` client、安装探测、启动保护、`/api/chat`、`/api/embed`、`/api/pull`、`/api/tags`、`/api/ps`、取消和最大响应保护已接入；主链 generation/embedding 改为 Ollama | 安装态探测、启动/停止、模型拉取、取消、应用内问答 E2E、索引换代、空闲驻留策略与异常恢复；评测示例、兼容枚举和旧错误码文案中的 llama.cpp 残留需清理或标注 |
| ONNX Session与GPU回退 | 开发完成（待安装烟测） | 当前主链 embedding 已由 Ollama 承担；ONNX 主要用于 rerank，旧 embedding 兼容路径仍在。历史 INT8 GPU/CPU 消融仅作背景，非本轮当前复验 | 干净安装包体积、纯CPU机器、驱动缺失、CUDA故障注入、非量化模型收益和长期资源争用 |
| sherpa-onnx生命周期 | 开发完成（待真机验证） | ASR、VAD 会话缓存、独立 speech sidecar、心跳/内存观测/Job Object 基础存在 | 真实录音、取消中断、故障注入和长期内存观测 |
| OCR生命周期 | 开发完成（待真机验证） | OCR sidecar、RapidOCR Session、线程预算、取消/心跳/内存观测基础存在 | 复杂OCR资源租约、真实文件矩阵和故障路径 |
| Windows Job Object | 开发完成（待故障注入） | ONNX/OCR/speech sidecar 已接入 Job Object 与级联治理基础 | 受控崩溃、长时间运行和安装态进程树验证 |
| 状态面板 | 开发完成（待体验） | 面板压缩为隐私、检索覆盖、授权位置、索引和推理资源；新增 Ollama Runtime 面板与 `/api/ps` 观测基础 | 实际高度、窄窗口、真实后端/模型和滚动体验 |

## 5. ASR与VAD

| 条目 | 状态 | 当前代码证据 | 未完成 |
|---|---|---|---|
| Worker运行环境 | 已完成 | `services/worker` 已锁定 sherpa-onnx 1.13.4 + sherpa-onnx-core（内置ONNX Runtime），并用 PyInstaller 打包进独立 speech sidecar `fanfan-worker.exe`，`WorkerClient::from_executable` 走通；ASR 模型 self_test 全过，应用内角色进程共享缺陷已修复 | 纳入安装包清单校验；真实录音链路与应用侧重验 |
| ASR目录与下载 | 开发完成（待体验） | 预设 ASR 默认 `sensevoice-small`（ModelScope 单源、sha256 已锁定），Paraformer+tokens+Silero VAD 保留兼容 | 正常下载、断点、取消、切源和磁盘故障实测 |
| VAD | 部分实现 | sherpa-onnx Silero VAD按512样本窗口裁剪静音，再交给ASR；用户不见独立模型卡 | 真实端点、弱音、噪声、长停顿和空录音语料 |
| ASR真实推理 | 部分实现 | Worker 按 `arch`（paraformer/sense_voice）创建 recognizer 并执行静音自检；前端显式录音、16k重采样、识别后回填输入框已接入 | 应用内真实录音→识别→回填链路待用户重验、中文/中英混合准确率语料待纳入评测 |
| 录音隐私 | 开发完成（待体验） | 只有点击后调用麦克风，界面显示录音/识别状态，停止后关闭轨道；不会自动发送 | 权限拒绝、应用失焦、页面离开和强制退出缓存检查 |
| 临时音频清理 | 部分实现 | 当前录音在内存，不写日志和知识库 | WebView/系统缓存生命周期人工审计 |
| 媒体转写 | 未实现 | Schema v19已有媒体转写与时间段表 | 显式开关、Media Foundation解码、检查点、FTS/Embedding入库和UI |

朗读（语音合成/TTS）已按功能评估结果整体移除（2026-08-20，见规划书 11.4）：不再包含语音合成开发计划、资源分配、验收标准与评测语料。ASR 的落地顺序与剩余验收（环境构建 → 模型自检 → 真实推理 → 隐私验收 → 评测闭环）见规划书 6.3.1 节。语音运行环境已就绪（独立 speech sidecar），应用内真实录音链路待用户重验；环境异常时语音按钮降级为禁用，不阻塞问资料主链路。

## 6. OCR与多模态文档

| 条目 | 状态 | 当前代码证据 | 未完成 |
|---|---|---|---|
| PP-OCRv5模型包 | 开发完成（待体验） | 检测4819576字节、识别16631306字节、方向585532字节、词典74012字节，均锁定SHA-256 | 在线下载安装流程和本机文件自检实测 |
| RapidOCR推理 | 开发完成（待体验） | 本地模型路径、ONNX Runtime、PP-OCRv5参数、线程预算、真实空白图自检、结构化错误 | 真实中文图片准确率与性能 |
| OCR定位契约 | 开发完成（待体验） | Worker输出每行text、confidence、polygon、bbox、page、engine和model_version；Schema v20持久化每次引擎尝试 | 真实文件定位、置信度/polygon与尝试链视觉体验 |
| 独立图片 | 开发完成（待体验） | 配置PP-OCRv5时优先运行；失败、空文本、缺置信度或低置信度时明确记录后再走Windows兼容路径 | 多格式、旋转、超大图和源哈希矩阵 |
| 扫描PDF | 部分实现 | Windows.Data.Pdf可仅渲染页面缓存，PP-OCRv5识别渲染图；失败可明确兼容回退 | 真实扫描PDF、页数限制、取消、缓存清理和bbox定位 |
| Office/PDF内嵌图 | 开发完成（待体验） | Schema v34状态机使内嵌图先进入图片OCR队列；OCR失败/不足或图表启发式命中后才进入VLM；任务带修订、缓存哈希、幂等键和来源定位，前台活动/core降级时让步 | 真实Office/PDF样本的提取覆盖、图表路由准确率、取消/断电恢复与整机流畅性 |
| Windows OCR回退 | 开发完成（待体验） | 回退不再静默，文件预览和收件箱失败项可展开引擎、状态、置信度、原因和安全错误码，日志只写安全链 | 语言包缺失、Paddle损坏和双故障路径 |
| 复杂版面OCR | 部分实现 | 低置信度、无置信度、数字密集图表、短行复杂布局和短文本视觉图已路由到本地VLM，并保留OCR文本作为提示 | PP-Structure表格/公式结构化运行时、受控真值集与路由误判率 |
| 图片Embedding与检索关联 | 开发完成（待体验） | OCR成功或VLM总结均原子生成带`image_asset_id`的节点、FTS与待Embedding块；全文/语义检索沿节点返回图片标识，前端直接显示只读缓存原图；历史ready图片自动回填 | 真实图片语义召回率、VLM总结质量、升级大库回填耗时和50万文件规模一致性 |

## 7. 数据、授权与内容处理

| 条目 | 状态 | 当前事实 | 未完成 |
|---|---|---|---|
| 首次明确授权 | 开发完成（待体验） | 可选文件夹、整盘或空资料库，未确认不注册根 | 全新FanFan数据目录零扫描实测 |
| 整盘硬排除 | 开发完成（待体验） | 系统、程序、凭据、应用数据、重解析点和自身目录规则存在 | 真实系统盘、云占位符、移动盘 |
| SQLite + USearch | 部分实现 | Schema v21、FTS、任务、会话、处理/OCR尝试链、扫描检查点和索引世代存在 | 当前资料完整索引、50万文件/500万块、损坏/锁库/断电恢复 |
| 分页与写协调 | 开发完成（待体验） | 文件/收件箱键集游标、交互写优先；扫描50项小批次，关系哈希在事务外，集合画像/候选计算在只读连接并按100条小事务写入 | 50万/55万级稳定推进、长时间并发与延迟 |
| 文本与代码切分 | 开发完成（待体验） | 格式专用结构节点和384/480/64分块基础存在 | 真实格式、tokenizer差异和定位矩阵 |
| 旧Office与ZIP | 部分实现 | 旧Office只读临时转换入口、ZIP只读清单 | LibreOffice真实矩阵；主动解压入口保持隐藏 |
| 源文件只读 | 开发完成（待体验） | 解析/缓存/索引只写应用数据；定向图片测试验证未修改 | 全流程抽样哈希零变化 |

## 8. 搜索、RAG、集合与收件箱

| 条目 | 状态 | 当前事实 | 未完成 |
|---|---|---|---|
| 单次混合搜索 | 开发完成（待体验） | 共享块级检索器组合文件名、FTS、Embedding、加权RRF、去重和MMR；范围向量使用USearch过取/补取，支持取消旧搜索；新增会话感知 Query Planner 确定性快路径，文档级 metadata 与 semantic 并行召回后做 RRF 融合。历史 MMR 性能修复仍作背景 | 当前资料Recall/MRR/nDCG、Rerank消融、大库延迟、资源抢占与 Query Planner 当前评测 |
| 多轮会话 | 开发完成（待体验） | 会话/消息持久化、加载、重命名、删除和历史理解基础存在 | 重启恢复和真实追问质量 |
| 严格引用RAG | 开发完成（待体验） | 主链强制 generation/embedding 可用；当前 generation/embedding 由本机 Ollama 承担，块级/文档级召回后以 JSON Schema 生成 `claims+citation_ids`；事实/拒答互斥，弱语义近邻不充当证据 | 当前资料48回合E2E、越权/无证据与100%事实引用门禁、0.6B质量瓶颈评估、真实 Ollama 问答验证 |
| 下载任务语义 | 部分实现 | 文件模型仍支持暂停/继续、失败后重试/切源/移除等语义；generation/embedding 经 Ollama `/api/pull` 拉取，支持流式 NDJSON 进度与取消，完成后登记就绪，不再写入本地文件模型仓库 | Ollama pull 不具备同等暂停/切源语义；真实并行下载、进程中取消、断电恢复仍待验证 |
| 持久模型仓库 | 开发完成（待体验） | 正式版模型仓库默认 `<安装目录>\model-store`，debug 默认仍使用开发安全路径；当前仅 rerank/ocr/asr 需要本地文件与 SHA-256，generation/embedding 改由 Ollama tag 托管 | 下载/取消/切源及应用重置的运行前后长期哈希，安装目录写入与回退验证 |
| Markdown与高亮 | 开发完成（待体验） | GFM渲染、问题关键词加粗和原文高亮存在 | 表格/长回答/暗色/引用定位体验 |
| 智能集合 | 部分实现 | 手动/规则/AI建议、共同主题命名、成员预览、确认/拒绝/排除基础存在 | 真实聚类质量、图片语义和大库增量 |
| 资料关系 | 部分实现 | 重复、版本、语义相关和包含/摘要类型存在；语义/集合 SQL 已从 SQLite 3.50 卡死中修复（见第9节根因行） | 文档Embedding近邻刷新与模型复核质量 |
| 收件箱失败闭环 | 开发完成（待体验） | 查看状态和解决状态拆分、失败页和按类型重试入口存在 | 历史失败迁移和真实重试E2E |

## 9. 真实资料评测与当前证据

| 条目 | 状态 | 当前事实 | 未完成 |
|---|---|---|---|
| 资料规模与扫描 | 部分验证 | 两个授权位置约2.71GB；新增目录实际枚举1649项，锁竞争修复后最近一次扫描已收口，当前总登记1655项 | 运行完整处理直到解析/OCR/Embedding/USearch队列稳定，再生成最终覆盖快照 |
| 扫描失败根因 | 已修复（待长稳） | 原失败批次耗时与1.5秒SQLite锁等待吻合；现已保留底层SQLite码、50项批提交、保存点隔离、退避重试和断点续跑，不再因单项或锁竞争回滚全部149项 | 锁故障注入与十分钟并发扫描/界面操作 |
| 分层覆盖率 | 开发完成（待体验） | 分别报告发现、可解析、已解析、失败、明确排除、FTS块、Embedding块、活动向量键和OCR/VLM待处理 | 当前资料处理完成后的真实数值与前端显示确认 |
| 评测闭环与 RAGAS 0.4.3（2026-08-20） | 部分实现 | v32/v34 评测持久化、Evidence-first 数据集、`evaluation_optimize` 与离线 Codex 判卷流程已接通；历史 DEV baseline 发生在当前 Ollama / Query Planner 改动前。当前格式与公开错误码契约已收口 | 用最新链路重跑 DEV/HOLDOUT；没有达到门槛前不声明综合分（详见规划书 11.2/11.3） |
| 工作区本地化（2026-08-20） | 已落地（范围需区分） | 评测根目录与 RAGAS 输出目录默认定向工作区 `DATA\FanFanEvaluation`，快照/导出/prepare 均落工作区且不随提交；正式运行数据/日志/模型默认改为安装目录，debug 默认避免污染源码目录 | 安装目录写入、不可写回退、旧 AppData 迁移与调试路径需随迭代复验 |
| 搜索评测 | 评测框架完成，未出有效分 | 已有确定性与内容派生搜索样本、Recall@10/MRR@10/nDCG@10、延迟和通道消融 | 60条真实用例与隐藏集；不得沿用旧的2用例临时分数 |
| RAG评测 | 评测框架完成，未出有效分 | 可跑24单轮、6组三轮追问和6负例；实际调用Embedding、混合召回、生成、结构修复、证据/权限/修订与支持校验 | 用现有模型经离线 Codex 判卷跑完，只报告新分数 |
| 关系与集合评测 | 部分实现 | 精确重复可程序生成真值；语义关系和集合建议可在加密快照运行，集合检查授权、成员数和非文件名命名 | 60对人工/确定性关系真值与12个主题簇；当前候选仍需升级为文档级USearch近邻和模型复核 |
| 关系/集合分析卡死根因 | 已修复（待长稳） | SQLite 3.50 bundled 对「CTE+EXISTS+LIMIT+外ORDER BY」默认内联使 semantic/collection SQL 灾难性重评估，已用 `AS MATERIALIZED` + 拆两查询、relation_groups UNIQUE 冲突、状态查询 TTL 缓存与 generation runtime 同模型复用修复 | 真实资料上长稳运行与剩余搜索链路延迟定位 |
| OCR/语音评测 | 部分实现 | OCR、ASR包和真实最小自检可运行；OCR→VLM路由已有普通OCR跳过、低置信进入VLM、图片问答引用和搜索返回原图的自动测试；ASR包含静音不幻觉检查 | 20张OCR/复杂图表真值、20段ASR、VLM总结质量与路由误判率 |
| 安全硬门禁 | 部分验证 | 模型11/11哈希通过；最近日志脱敏扫描未发现正文/问题/敏感绝对路径；评测器会比较源文件与模型载荷前后清单 | 完整评测前后源文件哈希、未授权目录、任务收口和FTS/Embedding/USearch一致性统一判定 |
| 综合隐藏集得分 | 未完成 | 没有足够真实运行证据，不声明达到85分 | DEV baseline 已就绪并待离线判卷；隐藏集综合分待最多三轮受控调优与 HOLDOUT 盲测，开发集提升且隐藏集不下降才接受 |

## 10. 桌面、日志与体验

| 条目 | 状态 | 当前事实 | 未完成 |
|---|---|---|---|
| GUI子系统/单实例/首帧后台 | 开发完成（待体验） | 发布GUI子系统、重复启动聚焦和后台初始化代码存在 | 新品牌可执行PE、双击、无控制台和冷启动实测 |
| 结构化错误 | 开发完成（待体验） | 前端统一规范化对象/Error/字符串，不应显示`[object Object]` | 全页面故障体验 |
| 运行日志 | 开发完成（待体验） | JSONL轮转、启动/前端/后台/操作/错误节点和脱敏基础存在 | 长时间体验完整性、性能与诊断包人工审查 |
| 主题与滚动 | 部分实现 | system/day/night和品牌色变量、滚动淡出基础存在 | 全页面视觉和用户品牌确认 |
| 状态面板 | 开发完成（待体验） | 约340px/52vh，只有隐私、覆盖、位置、索引和推理资源；关闭按钮固定右上，内部滚动 | 白天/夜晚、窄窗口、真实数据和键盘体验 |
| 安装包 | 未实现（本轮） | 当前旧安装包不是本代码 | 用户体验通过后才构建和烟测 |

## 11. 下一阶段任务

1. 清理或明确标注评测示例、前端/核心兼容枚举、旧错误码文案和兼容导入契约中的 llama.cpp/GGUF 残留，避免和主链 Ollama 口径冲突；
2. 补齐 Ollama 完整验证：安装态探测、启动/停止、`/api/pull` 进度/取消、应用内 chat/embed、切档后的索引重建与一次真实问答 E2E；
3. 用最新 Ollama + Query Planner + 文档级召回链路重跑 DEV/HOLDOUT pipeline；对工作区内 prepare 子集执行离线 Codex 判卷并 `import-codex-judge`，不调用云端评分；
4. 完成品牌零残留和小尺寸图标视觉检查，修正原图裁剪边界但不重新生成品牌图；
5. 补齐语义关系真值、集合主题簇、OCR/ASR受控语料和文件家族隐藏集；
6. 按消融结果优化切分、召回、RRF/MMR、Rerank、证据预算和结构化生成，隐藏集达到85分或输出明确瓶颈；
7. 用真实Office/PDF内嵌图和复杂图表验收已落地的OCR→VLM链路；复验VLM总结质量，并在未来更换非量化Rerank时重新做GPU/CPU消融；
8. 复验 ONNX/OCR/speech 独立 sidecar 的 Windows Job Object、心跳、取消与内存观测；
9. 实现显式媒体转写；
10. 逐项复验既有P0/P1/P2和日志，交付本地体验版本；
11. 用户明确允许后执行最终测试、安装包、Git历史重写和新仓库克隆验证。

### 11.1 ModelPreset → Runtime（主链已落地，尚未最终归档）

四档 preset 对齐、`preset_resolver`/`RuntimeModelPlan`、`apply_runtime_plan`、索引换代重建提示、SenseVoice/PP-OCRv6 下载源锁定与「下载源弹窗」、旧 `model_role_config`/清理功能死代码移除等主体已落地。当前主链又进一步切到 Ollama generation/embedding，并新增 Query Planner 与文档级召回；因此本项不能继续按“已归档”处理，需等真实 Ollama 问答、安装态存储、契约检查和当前 DEV/HOLDOUT 评测补齐后再归档。

## 12. 用户体验重点

体验阶段重点观察：

1. 图标和字标是否与原图一致，标题栏、欢迎页和小图标是否清晰；
2. 问答或后台模型运行时，浏览器和其他应用是否仍流畅；
3. 麦克风是否只在点击后启用，语音文本是否先确认再发送；
4. 多个模型任务是否保持紧凑稳定，暂停/继续/失败重试/切源/取消是否符合语义；重置后已安装模型是否完整；
5. PP-OCRv5下载、自检、图片/扫描PDF识别和“Paddle失败或低置信度 → Windows完成”是否真实；
6. 问资料是否真正经过混合检索和生成模型，引用失败时是否只显示错误而不展示摘录回答；
7. 多轮问答、Markdown、图片定位、集合、关系和收件箱是否可用；
8. 紧凑状态面板和日志是否能解释授权、索引、推理资源与错误。

出现问题时请保留日志并提供页面、操作顺序和大致时间。只有收到“验收通过，可以做最终测试”后，才执行完整回归、压力、真实模型矩阵、NSIS和安装烟测。

## 13. 暂缓项

云同步、NAS持续监听、移动端、插件、任意Agent、说话人分离和整段视频画面理解暂缓。它们不得阻塞本地授权、索引、搜索、预览、严格RAG和虚拟组织主链路。
