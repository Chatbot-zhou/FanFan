# FanFan / 翻翻项目协作约定

本仓库用于开发“翻翻”——完全本地化、源文件只读的 AI 信息管理助手。

## 工程实现原则

### 原子化设计

- 功能拆分为功能单一、接口清晰、可独立执行和验证的最小单元。
- 每个单元必须声明输入、输出、前置条件、后置条件、超时、重试、幂等键和错误类型。
- 单元之间只通过稳定契约传递数据，不共享隐式可变状态。
- 一个单元失败不得污染已验证结果，也不得触发未经授权的后续操作。

### 渐进验证

- 每个关键步骤完成后立即执行自动检查点，不把验证推迟到工作流末尾。
- 检查点至少覆盖 Schema、业务不变量、来源证据、权限、资源预算和输出完整性。
- 检查失败时只重试或替换当前单元；恢复时从最后一个有效检查点继续。

### 多路径探索

- 仅对高不确定性任务生成 2—3 条有明确预算的候选路径。
- 使用同一评价标准比较候选路径的正确性、证据、耗时和资源占用。
- 候选阶段不得产生不可逆副作用；选定路径后才进入后续步骤。
- 确定性任务不为形式上的“多路径”增加复杂度。

### 动态降级

- 持续观察内存、CPU、队列长度、延迟、超时和连续错误率。
- 异常时按 `full → balanced → core` 降级，优先暂停 OCR、复杂解析、多路径探索和生成式增强。
- 降级不得放宽目录授权、源文件只读、Schema 校验、引用要求或导出确认。
- 界面必须显示当前降级状态、触发原因和恢复条件；资源恢复后自动或经用户确认升级。

## 工程实现原则

### 原子化设计

- 功能拆分为功能单一、接口清晰、可独立执行和验证的最小单元。
- 每个单元必须声明输入、输出、前置条件、后置条件、超时、重试、幂等键和错误类型。
- 单元之间只通过稳定契约传递数据，不共享隐式可变状态。
- 一个单元失败不得污染已验证结果，也不得触发未经授权的后续操作。

### 渐进验证

- 每个关键步骤完成后立即执行自动检查点，不把验证推迟到工作流末尾。
- 检查点至少覆盖 Schema、业务不变量、来源证据、权限、资源预算和输出完整性。
- 检查失败时只重试或替换当前单元；恢复时从最后一个有效检查点继续。

### 多路径探索

- 仅对高不确定性任务生成 2—3 条有明确预算的候选路径。
- 使用同一评价标准比较候选路径的正确性、证据、耗时和资源占用。
- 候选阶段不得产生不可逆副作用；选定路径后才进入后续步骤。
- 确定性任务不为形式上的“多路径”增加复杂度。

### 动态降级

- 持续观察内存、CPU、队列长度、延迟、超时和连续错误率。
- 异常时按 `full → balanced → core` 降级，优先暂停 OCR、复杂解析、多路径探索和生成式增强。
- 降级不得放宽目录授权、源文件只读、Schema 校验、引用要求或导出确认。
- 界面必须显示当前降级状态、触发原因和恢复条件；资源恢复后自动或经用户确认升级。

## 开始工作前

1. 阅读 `docs/翻翻-V1-产品与开发规划书.md` 和 `docs/V1-方案逐条审查清单.md`；本项目不再维护用户旅程目录。
2. 执行 `git status --short --branch`，确认并保留用户已有改动。
3. 已连接远端时执行 `git fetch origin`；更新当前分支只能使用快进方式。
4. 不得用 `git reset --hard`、`git checkout --` 或清理命令丢弃未知改动。

## 分支规则

- `main` 始终保持可构建、可验证，不直接承载日常功能开发。
- 新工作默认从最新 `main` 创建 `codex/<类型>-<简短主题>` 分支。
- 类型使用 `feat`、`fix`、`docs`、`refactor`、`test`、`chore`。
- 示例：`codex/feat-folder-authorization`、`codex/docs-onboarding-node`。
- 一个分支只解决一个明确问题，禁止把无关改动混入同一提交或 PR。

## SSH 远端规则

- 所有 Git fetch、pull、push 和 submodule 操作必须使用 SSH。
- GitHub remote 必须使用 `git@github.com:<owner>/<repo>.git`，不得使用 HTTPS URL。
- 认证使用用户的 SSH key 与 ssh-agent；不得把访问令牌、密码或凭据写入 remote URL。
- 操作远端前执行 `git remote -v`，发现 HTTPS 地址时先改为 SSH。
- 引入子模块时 `.gitmodules` 也必须保存 SSH URL。
- SSH-only 约束针对 Git 数据传输；Issue、PR、Review、Actions 和 Release 使用 GitHub App 或 `gh` 按标准 GitHub API 流程管理。
- 不得因为使用 GitHub App 或 `gh` 而把仓库 remote 改成 HTTPS。

## 提交规则

- 提交信息使用 Conventional Commits：
  - `feat(scope): ...`
  - `fix(scope): ...`
  - `docs(scope): ...`
  - `refactor(scope): ...`
  - `test(scope): ...`
  - `chore(scope): ...`
- 提交前查看完整 diff，只暂存本次任务涉及的明确文件。
- 优先使用显式路径 `git add <file>`，工作区混杂时不得使用 `git add -A`。
- 每个提交必须能独立说明目的，并包含与风险相称的验证。
- 模型文件、索引、用户资料、数据库、日志、凭据和本机配置不得提交。

## 验证与发布

- 代码变更必须运行相关格式检查、静态检查、单元测试和必要的真实流程验证。
- 文档变更必须检查 UTF-8、相对链接、标题层级和字段契约一致性。
- 未经用户明确要求，不提交、推送、创建标签或发布版本。
- 发布时推送 `codex/*` 分支并创建 Draft PR；不得直接向远端 `main` 强制推送。
- PR、Issue、Review、Actions 检查和 Release 优先使用 GitHub App；需要本地分支或 Actions 日志时使用已认证的 `gh`。
- 合并前确认 CI、评审意见和产品文档均已更新。
- 宣称发布完成前，必须验证远端目标分支、提交和 `origin/main` 最终状态。

## 产品安全边界

- 源文件始终只读；应用不得移动、重命名、删除或覆盖用户文件。
- 业务导出必须由用户显式触发，新建文件且禁止覆盖现有目标。
- 未授权目录不可读取；模型、索引、日志和解析过程保持本地。
- 模型生成的结果必须经过 Schema、来源和权限校验。
